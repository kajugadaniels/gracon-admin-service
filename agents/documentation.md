# api/admin Documentation Rules

Update `README.md` when admin architecture, endpoints, environment variables, or integration boundaries change.

Update `.env.example` for every new variable.

Update Swagger decorators for every endpoint change.

Update `../../AGENTS.md` only when the cross-project architecture or service boundary changes.
